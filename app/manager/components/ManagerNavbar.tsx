'use client'

// This file is no longer used. The Manager portal now uses ManagerShell + ManagerSidebar (persistent rail).
// Kept for reference only.

export default function ManagerNavbar() {
  return null
}
